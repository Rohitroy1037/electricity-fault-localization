/**
 * Domain Event Types
 * 
 * Central registry of all internal domain events.
 * Prevents magic strings and ensures consistency across publishers and consumers.
 */
export const DomainEvents = {
  TELEMETRY_STORED: 'TelemetryStored',
};
